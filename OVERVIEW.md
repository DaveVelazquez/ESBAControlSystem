# 📊 Vista General del Sistema

## Sistema de Monitoreo de Técnicos en Campo

```
┌──────────────────────────────────────────────────────────────────────┐
│                  ARQUITECTURA DEL SISTEMA                            │
└──────────────────────────────────────────────────────────────────────┘

                          CLIENTES
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
   ┌────────┐          ┌─────────┐          ┌──────────┐
   │  Web   │          │ Mobile  │          │   API    │
   │ Admin  │          │  Tech   │          │Consumers │
   └────┬───┘          └────┬────┘          └─────┬────┘
        │                   │                     │
        └───────────────────┼─────────────────────┘
                            │
                            ▼
             ┌──────────────────────────┐
             │   Application Load       │
             │      Balancer (ALB)      │
             └─────────────┬────────────┘
                           │
            ┌──────────────┴───────────────┐
            │                              │
            ▼                              ▼
     ┌────────────┐                 ┌────────────┐
     │  Backend   │◄────────────────│  Backend   │
     │ API (ECS)  │     Replica     │ API (ECS)  │
     └──────┬─────┘                 └──────┬─────┘
            │                              │
            └──────────────┬───────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
   ┌────────┐        ┌─────────┐       ┌──────────┐
   │  RDS   │        │ Redis   │       │    S3    │
   │PostGre │        │ Cache   │       │Evidence/ │
   │  SQL   │        │ElastiCh.│       │  Files   │
   └────────┘        └─────────┘       └──────────┘
```

---

## 📁 Estructura de Archivos del Proyecto

```
Sistema de Control/
│
├── 📄 README.md                    # Documentación principal
├── 📄 QUICKSTART.md               # Guía de inicio rápido
├── 📄 RESUMEN_EJECUTIVO.md        # Resumen para stakeholders
├── 📄 PROJECT_STATUS.md           # Estado actual del proyecto
├── 📄 DEVELOPERS.md               # Guía para desarrolladores
├── 📄 docker-compose.yml          # Configuración Docker
├── 📄 setup.ps1                   # Script de setup automatizado
│
├── 📁 backend/                    # ✅ Backend API (100% completo)
│   ├── 📁 src/
│   │   ├── 📁 config/            # Configuración (DB, Redis)
│   │   │   └── database.js
│   │   ├── 📁 middleware/        # Middleware (auth, errors)
│   │   │   ├── auth.js
│   │   │   ├── errorHandler.js
│   │   │   └── rateLimiter.js
│   │   ├── 📁 routes/            # Rutas API (7 archivos)
│   │   │   ├── auth.js          # Login/Register
│   │   │   ├── orders.js        # CRUD Órdenes
│   │   │   ├── technicians.js   # Gestión técnicos
│   │   │   ├── locations.js     # Tracking GPS
│   │   │   ├── checkin.js       # Check-in/out
│   │   │   ├── evidences.js     # Fotos/Firmas
│   │   │   └── reports.js       # Reportes PDF
│   │   ├── 📁 services/          # Lógica de negocio
│   │   ├── 📁 utils/             # Utilidades
│   │   │   └── logger.js
│   │   └── server.js             # Entry point
│   ├── 📄 package.json           # Dependencias
│   ├── 📄 Dockerfile             # Config Docker
│   ├── 📄 .env.example           # Variables de entorno
│   └── 📄 README.md              # Docs backend
│
├── 📁 database/                   # ✅ Base de datos (100% completo)
│   ├── 📁 migrations/
│   │   └── 001_initial_schema.sql # Schema completo
│   └── 📁 seeds/
│       └── dev_data.sql          # Datos de prueba
│
├── 📁 docs/                       # ✅ Documentación (95% completo)
│   ├── API.md                    # Referencia completa API
│   └── DEPLOYMENT.md             # Guía deploy AWS
│
├── 📁 frontend-web/               # ⚠️ No iniciado
│   ├── 📁 public/
│   └── 📁 src/
│       ├── 📁 components/
│       ├── 📁 pages/
│       └── 📁 services/
│
├── 📁 mobile-app/                 # ⚠️ No iniciado
│   └── 📁 src/
│       ├── 📁 components/
│       ├── 📁 screens/
│       └── 📁 services/
│
└── 📁 infrastructure/             # ⚠️ No iniciado
    ├── 📁 lib/
    └── 📁 bin/
```

---

## 🔄 Flujo de Datos

### 1. Autenticación
```
User Input (email/password)
    │
    ▼
[POST /api/auth/login]
    │
    ├──► Validate Credentials
    │        │
    │        ▼
    │    Check Password (bcrypt)
    │        │
    │        ▼
    └──► Generate JWT Token
             │
             ▼
         Return Token + User Data
```

### 2. Creación de Orden
```
Dispatcher Creates Order
    │
    ▼
[POST /api/orders]
    │
    ├──► Validate Input
    ├──► Calculate SLA Deadline
    ├──► Generate Order Number
    │
    ▼
Insert into Database
    │
    ├──► orders table
    ├──► order_events table (audit)
    │
    ▼
Return Order Details
    │
    └──► Emit WebSocket Event
```

### 3. Asignación de Técnico
```
Assign Order to Technician
    │
    ▼
[POST /api/orders/assign]
    │
    ├──► Validate Technician Availability
    ├──► Check SLA Constraints
    ├──► Check Geographic Zone
    │
    ▼
Update Order Status
    │
    ├──► Set status = 'assigned'
    ├──► Set assigned_technician_id
    ├──► Log event
    │
    ▼
Send Push Notification (Firebase)
    │
    └──► Emit WebSocket Update
```

### 4. Check-in en Sitio
```
Technician Arrives at Site
    │
    ▼
[POST /api/orders/:id/checkin]
    │
    ├──► Get Current GPS Location
    ├──► Validate Distance to Site (<100m)
    ├──► Check No Active Check-in
    │
    ▼
Record Check-in Event
    │
    ├──► Insert order_events
    ├──► Update order.status = 'in_progress'
    ├──► Set order.actual_start
    │
    ▼
Return Success
    │
    ├──► Update Location Tracking
    └──► Notify Dispatcher (WebSocket)
```

### 5. Captura de Evidencias
```
Technician Takes Photo
    │
    ▼
[POST /api/orders/:id/evidences/photo]
    │
    ├──► Receive Image File
    ├──► Compress Image (Sharp)
    ├──► Generate Thumbnail
    │
    ▼
Upload to S3
    │
    ├──► Main image
    ├──► Thumbnail
    │
    ▼
Save to Database
    │
    ├──► evidences table
    │        │
    │        ├─ file_url
    │        ├─ thumbnail_url
    │        └─ metadata
    │
    └──► Return Evidence Details
```

### 6. Tracking en Tiempo Real
```
Mobile App (Every 2 min)
    │
    ▼
[POST /api/locations/ping]
    │
    ├──► Receive GPS Coordinates
    ├──► Validate Accuracy
    │
    ▼
Store in Database
    │
    └──► technician_locations table
             │
             ▼
         Emit via WebSocket
             │
             └──► All Connected Dispatchers
```

---

## 📊 Modelo de Base de Datos

```
┌─────────────┐         ┌──────────────┐         ┌─────────────┐
│    users    │         │    orders    │         │   clients   │
├─────────────┤         ├──────────────┤         ├─────────────┤
│ id (PK)     │◄───┐    │ id (PK)      │    ┌───►│ id (PK)     │
│ email       │    │    │ order_number │    │    │ name        │
│ password    │    │    │ client_id FK │────┘    │ email       │
│ name        │    │    │ site_id FK   │────┐    │ phone       │
│ role        │    │    │ technician FK│    │    └─────────────┘
│ phone       │    └────│ status       │    │
└─────────────┘         │ priority     │    │    ┌─────────────┐
                        │ sla_deadline │    └───►│    sites    │
┌─────────────┐         │ actual_start │         ├─────────────┤
│technician_  │         │ actual_end   │         │ id (PK)     │
│ profiles    │         └──────────────┘         │ client_id FK│
├─────────────┤                │                 │ address     │
│ id (PK)     │                │                 │ latitude    │
│ user_id FK  │                │                 │ longitude   │
│ skills      │                │                 └─────────────┘
│ rating      │                │
└─────────────┘                │
                               │
                    ┌──────────┴───────────┐
                    │                      │
                    ▼                      ▼
            ┌──────────────┐      ┌──────────────┐
            │order_events  │      │  evidences   │
            ├──────────────┤      ├──────────────┤
            │ id (PK)      │      │ id (PK)      │
            │ order_id FK  │      │ order_id FK  │
            │ event_type   │      │ type         │
            │ latitude     │      │ file_url     │
            │ longitude    │      │ signature    │
            │ timestamp    │      │ notes        │
            └──────────────┘      └──────────────┘

┌──────────────────┐
│ technician_      │
│   locations      │
├──────────────────┤
│ id (PK)          │
│ technician_id FK │
│ order_id FK      │
│ latitude         │
│ longitude        │
│ accuracy         │
│ timestamp        │
│ is_active        │
└──────────────────┘
```

---

## 🚦 Estados de Orden

```
  ┌─────────┐
  │ Pending │  ◄─── Orden creada
  └────┬────┘
       │
       │ Assign Technician
       ▼
  ┌──────────┐
  │ Assigned │  ◄─── Asignada a técnico
  └────┬─────┘
       │
       │ Technician Accepts
       ▼
  ┌──────────┐
  │ En Route │  ◄─── En camino al sitio
  └────┬─────┘
       │
       │ Check-in at Site
       ▼
  ┌─────────────┐
  │ In Progress │  ◄─── Trabajando en sitio
  └─────┬───────┘
       │
       │ Check-out
       ▼
  ┌───────────┐
  │ Completed │  ◄─── Trabajo terminado
  └───────────┘

       ┌──────────┐
       │ On Hold  │  ◄─── En pausa (opcional)
       └──────────┘

       ┌───────────┐
       │ Cancelled │  ◄─── Cancelada
       └───────────┘
```

---

## 🎨 Roles y Permisos

```
┌──────────────────────────────────────────────────────────┐
│                         ADMIN                            │
├──────────────────────────────────────────────────────────┤
│ ✅ Full Access                                           │
│ ✅ Manage Users                                          │
│ ✅ Configure System                                      │
│ ✅ View All Reports                                      │
│ ✅ Access All Endpoints                                  │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                      DISPATCHER                          │
├──────────────────────────────────────────────────────────┤
│ ✅ Create Orders                                         │
│ ✅ Assign Technicians                                    │
│ ✅ Monitor Real-time Tracking                            │
│ ✅ View Reports                                          │
│ ✅ Communicate with Technicians                          │
│ ❌ Manage Users                                          │
│ ❌ System Configuration                                  │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                      TECHNICIAN                          │
├──────────────────────────────────────────────────────────┤
│ ✅ View Assigned Orders                                  │
│ ✅ Check-in / Check-out                                  │
│ ✅ Upload Evidences                                      │
│ ✅ Update Location                                       │
│ ✅ Add Notes                                             │
│ ❌ Create Orders                                         │
│ ❌ Assign Orders                                         │
│ ❌ View Other Technicians' Data                          │
└──────────────────────────────────────────────────────────┘
```

---

## 💻 Tecnologías Utilizadas

### Backend
```
┌─────────────────────────────────────────┐
│ Runtime:     Node.js 18                 │
│ Framework:   Express.js 4.18            │
│ Database:    PostgreSQL 14 + PostGIS    │
│ Cache:       Redis 7                    │
│ Auth:        JWT (jsonwebtoken)         │
│ Real-time:   Socket.IO 4.6              │
│ Logging:     Winston 3.11               │
│ Security:    Helmet, bcrypt, CORS       │
│ Validation:  express-validator          │
│ Images:      Sharp (compression)        │
│ Cloud:       AWS SDK (S3, etc)          │
└─────────────────────────────────────────┘
```

### Frontend (Planeado)
```
┌─────────────────────────────────────────┐
│ Framework:   React 18 + TypeScript      │
│ State:       Redux Toolkit / Zustand    │
│ UI:          Material-UI / Ant Design   │
│ Maps:        Mapbox GL JS               │
│ Charts:      Recharts / Chart.js        │
│ HTTP:        Axios                      │
│ WebSocket:   Socket.IO Client           │
└─────────────────────────────────────────┘
```

### Mobile (Planeado)
```
┌─────────────────────────────────────────┐
│ Framework:   React Native + TypeScript  │
│ Navigation:  React Navigation           │
│ State:       Redux Toolkit              │
│ Maps:        @react-native-mapbox-gl    │
│ Camera:      react-native-camera        │
│ Storage:     AsyncStorage               │
│ Network:     Axios                      │
│ Offline:     Redux Persist              │
│ Push:        Firebase Cloud Messaging   │
└─────────────────────────────────────────┘
```

### DevOps & Infrastructure
```
┌─────────────────────────────────────────┐
│ Containers:  Docker + Docker Compose    │
│ Cloud:       AWS (ECS, RDS, S3, CF)     │
│ IaC:         AWS CDK (TypeScript)       │
│ CI/CD:       GitHub Actions             │
│ Monitoring:  CloudWatch                 │
│ Secrets:     AWS Secrets Manager        │
└─────────────────────────────────────────┘
```

---

## 📈 Métricas y KPIs

### Métricas Técnicas
- ⏱️ **API Response Time**: <200ms p95
- 🔄 **Uptime**: 99.9%
- 📊 **Throughput**: 100 req/s
- 💾 **Database Queries**: <50ms avg
- 🔒 **Failed Logins**: <1%

### Métricas de Negocio
- 📋 **Orders Completed**: Daily/Weekly/Monthly
- ⏰ **SLA Compliance**: % on-time completions
- 👷 **Technician Utilization**: % active time
- 📸 **Evidence Capture Rate**: % orders with photos
- ⭐ **Customer Satisfaction**: Avg rating

---

## 🔐 Seguridad Implementada

```
┌─────────────────────────────────────────────────────────┐
│                   CAPA DE SEGURIDAD                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 1. Authentication (JWT)                          │  │
│  │    - Token-based auth                            │  │
│  │    - 7-day expiration                            │  │
│  │    - Refresh token support                       │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 2. Authorization (RBAC)                          │  │
│  │    - Role-based access control                   │  │
│  │    - Route-level permissions                     │  │
│  │    - Resource ownership validation               │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 3. Input Validation                              │  │
│  │    - express-validator                           │  │
│  │    - Schema validation                           │  │
│  │    - SQL injection prevention                    │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 4. Rate Limiting                                 │  │
│  │    - 100 req/15min (general)                     │  │
│  │    - 5 req/15min (auth endpoints)                │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 5. Encryption                                    │  │
│  │    - Passwords: bcrypt (12 rounds)               │  │
│  │    - HTTPS/TLS 1.2+                              │  │
│  │    - Data at rest (RDS encryption)               │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 6. Security Headers (Helmet.js)                  │  │
│  │    - XSS Protection                              │  │
│  │    - CSRF Prevention                             │  │
│  │    - Content Security Policy                     │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📱 Interfaz de Usuario (Planeada)

### Web Dashboard
```
┌────────────────────────────────────────────────────────┐
│  [Logo] Field Service Manager    [User] [Notifications]│
├────────────────────────────────────────────────────────┤
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │         MAPA EN TIEMPO REAL                      │ │
│  │  🗺️  [Técnicos en mapa con marcadores]          │ │
│  │      [Órdenes activas]                           │ │
│  │      [Clustering para densidad]                  │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  ┌─────────────┐  ┌─────────────┐  ┌───────────────┐ │
│  │ Orders: 45  │  │ Active: 12  │  │ SLA Alert: 3  │ │
│  └─────────────┘  └─────────────┘  └───────────────┘ │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │ ÓRDENES ACTIVAS                    [+ Nueva]     │ │
│  ├──────────────────────────────────────────────────┤ │
│  │ ORD-001  TechCorp    Juan T.   🟢 En progreso   │ │
│  │ ORD-002  RetailMax   Carlos M. 🟡 En camino     │ │
│  │ ORD-003  FoodSvcs    Ana L.    🔴 Vencido       │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
└────────────────────────────────────────────────────────┘
```

### Mobile App
```
┌──────────────────────┐
│  📱 Field Tech App   │
├──────────────────────┤
│                      │
│  Mis Órdenes - Hoy   │
│  ┌────────────────┐  │
│  │ ORD-001        │  │
│  │ TechCorp       │  │
│  │ 10:00 - 12:00  │  │
│  │ [Ver Detalles] │  │
│  └────────────────┘  │
│                      │
│  ┌────────────────┐  │
│  │ ORD-002        │  │
│  │ RetailMax      │  │
│  │ 14:00 - 16:00  │  │
│  │ [Ver Detalles] │  │
│  └────────────────┘  │
│                      │
│  [Navegación]        │
│  🏠 📋 📍 👤         │
└──────────────────────┘
```

---

¡Documentación Visual Completa! 🎉
